## Current Feature
ALL FEATURES PASSED — Module 2 complete (6/6)

## Feature #6 Status
feature_id: 6
description: Workspace 页面启动 Daemon 按钮 + __dirname 修复
status: done
dispatched: 2026-05-29
accepted: 2026-05-29

## Previous Status
feature_id: 5
status: done
accepted: 2026-05-28

## Dev Delivery

### Feature #1: Web 工程初始化 + initWorkspace 目录骨架函数（2026-05-25）

**实现功能**：
1. 在 `web/` 目录创建 Next.js 工程（App Router、TypeScript、src/ 目录）
2. 实现 `initWorkspace(workspacePath)` 函数，调用后生成完整的 Team3 项目目录骨架
3. 生成文件：spec/app_design.md、spec/agents/、spec/actions.jsonl、spec/decision_log.md、spec/modules_progress.json、.team3-project.json
4. .team3-project.json 符合 app_design.md 定义的 schema（name、createdTime、workspace、init_workspace=true、partner 结构）
5. 重复调用完全幂等，不覆盖已有文件

**新建/修改文件**：
- `web/package.json` — Next.js 工程配置（含 vitest 测试脚本）
- `web/tsconfig.json` — TypeScript 配置
- `web/vitest.config.ts` — 单元测试配置
- `web/vitest.config.e2e.ts` — 集成测试配置
- `web/src/lib/init/init-workspace.ts` — initWorkspace 核心实现
- `web/src/lib/init/index.ts` — 模块导出
- `web/test/init-workspace.test.ts` — 16 个单元测试（全部 mock fs）
- `web/e2e/feature_1/test1_init_workspace.ts` — 22 个 e2e 测试（真实文件系统验证）
- `web/init.sh` — Dev 环境启动脚本

**测试结果**：
- 单元测试：16/16 通过
- E2E 测试：22/22 通过（覆盖全部 4 个 checkpoint steps）
- TypeScript 编译：零错误

### Feature #2: Agent prompt 模板内容定义、新建项目 copy（2026-05-25）

**实现功能**：
1. 在 `web/src/lib/init/templates/` 建立 agent prompt 模板目录，包含 team3.md、arch_prompt.md、dev_prompt.md、uat_prompt.md
2. 模板内容从本项目 `spec/agents/` 下对应文件拷贝（完整协议内容）
3. 修改 `initWorkspace` 函数，在创建 `spec/agents/` 后将 4 个模板文件 copy 到目标项目（幂等不覆盖）
4. team3.md 包含完整工作流协议（团队构成、核心文件说明、actions.jsonl schema、reread 协议）
5. arch/dev/uat 均包含「建立项目全局认识」按序读指令（team3.md → app_design.md → decision_log.md → modules_progress.json）
6. arch/dev/uat 均包含 decision_log.md 写入触发条件和格式要求
7. arch/dev/uat 均包含 actions.jsonl 写入规范（引用 team3.md 中的字段 schema + reread 末尾协议）

**新建/修改文件**：
- `web/src/lib/init/templates/team3.md` — 工作流协议模板
- `web/src/lib/init/templates/arch_prompt.md` — Architect prompt 模板
- `web/src/lib/init/templates/dev_prompt.md` — Dev prompt 模板
- `web/src/lib/init/templates/uat_prompt.md` — UAT prompt 模板
- `web/src/lib/init/init-workspace.ts` — 新增模板 copy 逻辑
- `web/test/init-workspace.test.ts` — 更新单元测试（增加模板 copy 断言）
- `web/e2e/feature_2/test1_agent_templates.ts` — 33 个 e2e 测试
- `web/vitest.config.e2e.ts` — 添加 fileParallelism: false 避免测试冲突

**测试结果**：
- 单元测试：21/21 通过
- E2E 测试：55/55 通过（Feature #1: 22 + Feature #2: 33，覆盖全部 5 个 checkpoint steps）
- TypeScript 编译：零错误
- 回归检查：Feature #1 e2e 22/22 全绿

### Feature #3: startDaemon 启动函数（2026-05-25）

**实现功能**：
1. `web/src/lib/init/start-daemon.ts` 导出 `startDaemon(workspacePath, options?)` 函数
2. 通过 `child_process.spawn` 启动 daemon 进程（入口 `daemon/src/daemon.js`），传递 `DAEMON_PORT` 和 `TEAM3_PROJECT_JSON` 环境变量
3. 启动后更新 `.team3-project.json` 的 `init_daemon` 字段为 daemon PID
4. 函数内部通过 WebSocket 连接等待 daemon 就绪（收到 `{type:"connected"}` 消息后 resolve）
5. 启动失败场景正确 reject：daemon 入口不存在、spawn 失败、进程退出（如端口占用 EADDRINUSE）、WebSocket 连接超时
6. 使用 `Promise.race` 检测 child 进程提前退出（防止 WS 连接到其他进程误判成功）

**新建/修改文件**：
- `web/src/lib/init/start-daemon.ts` — startDaemon 核心实现
- `web/src/lib/init/index.ts` — 新增 startDaemon 导出
- `web/test/start-daemon.test.ts` — 7 个单元测试（mock child_process/ws/fs）
- `web/e2e/feature_3/test1_start_daemon.ts` — 5 个 e2e 测试（真实启动 daemon）
- `web/package.json` — 新增 ws + @types/ws 依赖

**测试结果**：
- 单元测试：28/28 通过（Feature #1: 21 + Feature #3: 7）
- E2E 测试：60/60 通过（Feature #1: 22 + Feature #2: 33 + Feature #3: 5，全部真实启动 daemon 无 mock）
- TypeScript 编译：零错误
- 回归检查：Feature #1/#2 e2e 全绿

### Feature #4: initAgents 初始化 arch + uat Agent（2026-05-25）

**实现功能**：
1. 创建 `daemon/src/orchestrator-entry.js` — DaemonOrchestrator 的独立入口脚本，支持通过环境变量配置端口、路径、stub-claude
2. 实现 `web/src/lib/init/init-agents.ts` 导出 `initAgents(workspacePath, options?)` 函数
3. initAgents 通过 startDaemon 启动 DaemonOrchestrator（完整管线：WS + ActionWatcher + AgentScheduler + MessageRouter）
4. 写入 actions.jsonl 触发 daemon 调度：`to_arch`（路由到 arch 队列）和 `uat_check`（路由到 uat 队列）
5. AgentScheduler._resolveSession 自动生成 UUID v4 并写入 .team3-project.json
6. stub-claude 模拟 arch 行为：检测 prompt 含 "actions.jsonl" 后写入 "已在线" 通知
7. initAgents 轮询等待：session UUIDs 出现 + arch 上线通知，全部就绪后 resolve
8. 返回 `{ arch: { sessionId }, uat: { sessionId }, daemonPid }`

**新建/修改文件**：
- `daemon/src/orchestrator-entry.js` — DaemonOrchestrator 独立入口（支持 STUB_CLAUDE_PATH 等 env）
- `web/src/lib/init/init-agents.ts` — initAgents 核心实现
- `web/src/lib/init/index.ts` — 新增 initAgents 及类型导出
- `web/test/init-agents.test.ts` — 10 个单元测试（全部 mock fs/startDaemon）
- `web/e2e/feature_4/test1_init_agents.ts` — 7 个 e2e 测试（真实启动 orchestrator + stub-claude）

**测试结果**：
- 单元测试：38/38 通过（Feature #1: 21 + Feature #3: 7 + Feature #4: 10）
- E2E 测试：67/67 通过（Feature #1: 22 + Feature #2: 33 + Feature #3: 5 + Feature #4: 7）
- TypeScript 编译：零错误
- 回归检查：Feature #1/#2/#3 e2e 全绿


### Feature #6: Workspace 页面启动 Daemon 按钮 + __dirname 修复（2026-05-29）

**实现功能**：
1. `start-daemon.ts` resolveDaemonEntry() 从 `__dirname` 改为 `process.cwd()` 解析 daemon 入口路径（`path.join(process.cwd(), "..", "daemon", "src", "daemon.js")`）
2. `init-agents.ts` resolveOrchestratorEntry() 同样改为 `process.cwd()`（`path.join(process.cwd(), "..", "daemon", "src", "orchestrator-entry.js")`）
3. 新增 `POST /api/project/start` API：接收 `{ workspace }` body，内部通过 `process.cwd()` 解析 orchestrator-entry.js 路径，调用 `startDaemon` 启动 daemon，返回 `{ success, pid, port }`
4. ChatPanel 状态栏：daemon 状态为 disconnected 时显示「启动 Daemon」按钮，点击后 POST /api/project/start，显示 loading 状态。成功后 useDaemonSocket 通过 backoff 自动重连，按钮消失
5. API 完整错误处理：workspace 缺失/非绝对路径 → 400，.team3-project.json 不存在 → 404，startDaemon 失败 → 500

**修改文件**：
- `web/src/lib/init/start-daemon.ts` — resolveDaemonEntry() 去掉 __dirname，改用 process.cwd()；函数签名去掉 workspacePath 参数
- `web/src/lib/init/init-agents.ts` — resolveOrchestratorEntry() 去掉 __dirname，改用 process.cwd()
- `web/src/components/ChatPanel.tsx` — 新增 startingDaemon 状态 + handleStartDaemon 函数 + 条件渲染「启动 Daemon」按钮
- `web/src/app/globals.css` — 新增 .chat-start-daemon-btn 样式

**新建文件**：
- `web/src/app/api/project/start/route.ts` — POST /api/project/start API 实现
- `web/test/api-project-start.test.ts` — 9 个单元测试（mock startDaemon/fs）
- `web/e2e/m2_feature_6/test1_start_daemon_api.ts` — 10 个 e2e 测试（真实 HTTP API + 真实 daemon 启动 + ws 连接）

**集成测试覆盖 Checkpoint**：
- Step 1: ✅ start-daemon.ts resolveDaemonEntry 函数体不含 __dirname，含 process.cwd()（1 条验证）
- Step 1: ✅ init-agents.ts resolveOrchestratorEntry 函数体不含 __dirname，含 process.cwd()（1 条验证）
- Step 1: ✅ 两个函数都引用 daemon/src/ 下的正确入口文件名（1 条验证）
- Step 2: ✅ POST /api/project/start 缺 workspace → 400，非绝对路径 → 400，.team3-project.json 不存在 → 404（3 条验证）
- Step 4: ✅ POST /api/project/start { workspace } → 200 + success + pid + port（1 条验证）
- Step 4: ✅ daemon 进程存活确认（signal 0 检查）（1 条验证）
- Step 5: ✅ ws://127.0.0.1:<port> 连接成功并收到 { type: "connected" } 消息（1 条验证）
- Step 5: ✅ .team3-project.json init_daemon 字段已更新为 daemon PID（1 条验证）

**测试结果**：
- 单元测试：172 passed / 14 files（新增 9 cases / 1 file）
- 集成测试：10 passed（全部通过真实 Next.js HTTP API + 真实 daemon 启动）
- TypeScript: tsc --noEmit --skipLibCheck 零错误
- 回归检查：Feature #5 e2e 14/14 通过，Feature #8 e2e 25/25 通过

### Feature #5: 修复 initWorkspace 模板 copy 失败（2026-05-28）

**根因**：
getTemplatesDir() 使用 `__dirname` 做路径解析，Turbopack 编译后 `__dirname` 变为虚拟路径 `/ROOT/src/lib/init`，导致 `fs.existsSync` 返回 false 静默跳过模板 copy。Feature #2 的 e2e 通过 vitest 直接 import initWorkspace（不经 Next.js 编译）所以 `__dirname` 正常工作，掩盖了此问题。

**修复内容**：
1. 将 `dynamics/templates/`（agent/ + uat/ 子目录）整体复制到 `dynamics/web/templates/`
2. `getTemplatesDir()` 改为 `path.join(process.cwd(), "templates")`，不再使用 `__dirname`
3. 修复单元测试 mock：旧 mock 匹配 `/templates/<file>` 路径，实际结构为 `/templates/agent/<file>` 和 `/templates/uat/<file>`，导致 5 个测试 pre-existing 失败
4. 新增 UAT scaffold 文件的单元测试覆盖（4 个 copy + idempotency）

**新建文件**：
- `web/templates/` — 从 `dynamics/templates/` 复制（agent/ 4 文件 + uat/ 4 文件）
- `web/e2e/m2_feature_5/test1_template_copy.ts` — 14 个 e2e 测试（通过真实 Next.js HTTP API 验证）

**修改文件**：
- `web/src/lib/init/init-workspace.ts` — getTemplatesDir() 改用 process.cwd()
- `web/test/init-workspace.test.ts` — 修复 mock 路径匹配（templates/agent/, templates/uat/），新增 UAT scaffold 测试，从 21 cases 增至 26 cases

**集成测试覆盖 Checkpoint**：
- Step 1: ✅ web/templates/ 存在 agent/ 和 uat/ 子目录，各含 4 个非空文件（2 条验证）
- Step 2: ✅ init-workspace.ts getTemplatesDir 函数体不含 __dirname，含 process.cwd()（1 条验证）
- Step 3: ✅ POST /api/project/init 后 spec/agents/ 包含 team3.md/arch_prompt.md/dev_prompt.md/uat_prompt.md，全部非空且 >100 字符（4 条验证）
- Step 4: ✅ POST /api/project/init 后 uat/ 包含 simulate_human.mjs/logger.mjs/browser.mjs/watchdog.mjs，全部非空且 >50 字符（4 条验证）
- Step 5: ✅ 通过真实 Next.js HTTP API 调用（非 vitest 直接 import），验证 .team3-project.json 和模板内容与源文件一致（2 条验证 + 1 条内容比对）

**测试结果**：
- 单元测试：163 passed / 13 files passed（含修复后的 init-workspace 26 cases 全部通过）
- 集成测试：14 passed（全部通过真实 Next.js HTTP API）
- TypeScript: tsc --noEmit --skipLibCheck 零错误
- 回归检查：Module 1 Feature #8 e2e 25/25 通过

## Architect Notes

### Feature #6 验收通过（2026-05-29）— Module 2 全部完成
- **独立审查判断**：三处改动均正确：(1) resolveDaemonEntry 从 `path.resolve(__dirname, "../../../../daemon/src/daemon.js")` 改为 `path.join(process.cwd(), "..", "daemon", "src", "daemon.js")`；(2) resolveOrchestratorEntry 同理改为 process.cwd()；(3) POST /api/project/start 在 API 层自行解析 orchestrator-entry.js 路径并传入 daemonEntryPath option，不依赖 startDaemon 内部默认解析。ChatPanel 按钮逻辑简洁，disconnected 时显示、startingDaemon 状态防重复点击、成功后 useDaemonSocket backoff 自动重连。
- **对抗式 checklist**：
  - 单测 assert 完整性：已检查，9 cases 覆盖全部 API 分支（400×3/404/500/200），含 daemonEntryPath 不含 __dirname 的断言
  - e2e mock 被测主体：已检查，通过真实 HTTP API → 真实 spawn orchestrator → 真实 WebSocket 连接收到 connected 消息，零 mock
  - tautology：未发现。PID 由真实 spawn 产生，WS connected 由真实 daemon 发送，process.kill(pid, 0) 检查真实进程存活
  - 异常/边界场景：已检查，workspace 缺失/非绝对路径/.team3-project.json 不存在/startDaemon 失败 均有覆盖
  - 跨 feature 复用：已检查，复用 Feature #3 startDaemon 函数（不重写 spawn 逻辑），复用 Module 1 useDaemonSocket hook，API 校验模式与其他 route 一致
- **抽测 e2e**：`web/e2e/m2_feature_6/test1_start_daemon_api.ts` — 10/10 通过（929ms）。首次跑因旧 daemon 进程占端口 3100 导致 EADDRINUSE 失败 4 个，kill 旧进程后重跑全绿
- **疑点/风险**：端口 3100 硬编码为默认值。若用户同时开多个项目，各自的 daemon 会抢同一个端口导致 EADDRINUSE。当前单项目场景可接受，多项目需引入端口分配机制

### Feature #5 验收通过（2026-05-28）— Module 2 全部完成（修复后）
- **独立审查判断**：根因准确——Turbopack 编译后 `__dirname` 变为虚拟路径 `/ROOT/src/lib/init`，导致 getTemplatesDir 返回的目录不存在，fs.existsSync 静默跳过模板 copy。修复方案正确：(1) templates 从 dynamics/ 迁移到 web/ 下；(2) getTemplatesDir 改用 process.cwd() 替代 __dirname。代码改动精准，只动了 getTemplatesDir 函数体一行。
- **关键验证**：手动 curl POST /api/project/init 创建项目后，检查 spec/agents/（4 文件全部存在且非空）和 uat/（4 文件全部存在且非空），确认模板通过真实 Turbopack 编译路径成功 copy。
- **对抗式 checklist**：
  - 单测 assert 完整性：已检查，26 cases 覆盖 skeleton + agent 模板 + UAT scaffold + 幂等性。mock 路径从 `/templates/<file>` 修正为 `/templates/agent/<file>` 和 `/templates/uat/<file>`，与实际目录结构一致
  - e2e mock 被测主体：已检查，14 个 e2e 全部通过真实 Next.js HTTP API（fetch POST /api/project/init），不使用 vitest 直接 import——这正是本 Feature 的核心：原 Feature #2 e2e 用 vitest 直接 import 掩盖了 __dirname 问题
  - tautology：未发现。Step 1 检查静态文件存在、Step 2 检查源码不含 __dirname、Steps 3-5 通过 HTTP API 创建项目后验证磁盘文件
  - 异常/边界场景：对于 bug fix 类 feature，核心是"修复是否生效"而非新增边界。e2e 覆盖了完整的 HTTP→Turbopack 编译→initWorkspace→模板 copy 链路
  - 跨 feature 复用：已检查，修改的是 Feature #2 原有的 getTemplatesDir 函数，未平行实现
- **抽测 e2e**：`web/e2e/m2_feature_5/test1_template_copy.ts` — 14/14 通过（466ms）
- **疑点/风险**：dynamics/templates/ 原目录仍然存在（与 web/templates/ 重复），虽不影响功能但可能造成后续维护混淆。建议后续清理。另外 process.cwd() 依赖 Next.js 进程从 web/ 目录启动的约定——如果未来部署方式改变（如 Docker 中 cwd 不是 web/），需重新审视此假设。

### Feature #4 验收通过（2026-05-25）— Module 2 全部完成
- **独立审查判断**：initAgents 启动真实 DaemonOrchestrator 全管线（WS+ActionWatcher+AgentScheduler+MessageRouter），通过写 actions.jsonl 触发 daemon 调度链生成 agent session。stub-claude 作为外部依赖替身合理（claude CLI 需 API key），与 Module 3 e2e 设计一致
- **Checkpoint Step 2 说明**：checkpoint 写"通过 WebSocket 向 daemon 发送"，实际用 actions.jsonl 触发 daemon 调度。因为 daemon 设计接口就是文件监听，Arch 在派发时已明确指导此方式，功能等价
- **对抗式 checklist**：
  - 单测 assert 完整性：已检查，10 项覆盖 poll 超时/session 解析/通知检测/错误路径
  - e2e mock 被测主体：已检查，真实启动 orchestrator 全管线（ActionWatcher 文件监听 → AgentScheduler 队列调度 → spawn stub-claude），stub-claude 是外部依赖替身非被测主体
  - tautology：未发现，UUID 由 AgentScheduler 生成，"已在线"由 stub-claude 响应调度链写入
  - 跨 feature 复用：已检查，内部调用 Feature #3 startDaemon + e2e 用 Feature #1 initWorkspace
- **抽测 e2e**：`web/e2e/feature_4/test1_init_agents.ts` — 7/7 通过（耗时 1.77s）

### Feature #3 验收通过（2026-05-25）
- **独立审查判断**：startDaemon 通过 spawn+detached 启动 daemon，Promise.race 同时监听 WS 就绪和进程退出，错误处理覆盖入口不存在/spawn 失败/进程退出/WS 超时四种场景，PID 写入 .team3-project.json 正确
- **对抗式 checklist**：
  - 单测 assert 完整性：已检查，7 项覆盖核心逻辑分支
  - e2e mock 被测主体：已检查，真实 spawn daemon + 真实 WS 连接，零 mock
  - tautology：未发现，断言基于真实进程和网络状态
  - 跨 feature 复用：已检查，复用 Feature #1 的 initWorkspace 准备测试环境
- **抽测 e2e**：`web/e2e/feature_3/test1_start_daemon.ts` — 5/5 通过（耗时 3s，确认真实启动）

### Feature #2 验收通过（2026-05-25）
- **独立审查判断**：模板目录 web/src/lib/init/templates/ 包含完整的 4 个 prompt 文件，内容与本项目 spec/agents/ 一致；initWorkspace 通过 readFileSync+writeFileSync 实现 copy 逻辑，幂等检查无遗漏
- **对抗式 checklist**：
  - 单测 assert 完整性：已检查，新增的模板 copy 断言有效
  - e2e mock 被测主体：已检查，直接操作真实 fs 无 mock
  - tautology：未发现，断言检查模板内容中的协议关键词
  - 跨 feature 复用：已检查，扩展了 Feature #1 的 initWorkspace 函数而非平行实现
- **抽测 e2e**：`web/e2e/feature_2/test1_agent_templates.ts` — 33/33 通过

### Feature #1 验收通过（2026-05-25）
- **独立审查判断**：initWorkspace 函数实现简洁正确，skeleton 文件列表完整覆盖 checkpoint 要求，幂等逻辑通过 `existsSync` 前置检查实现无遗漏
- **对抗式 checklist**：
  - 单测 assert 完整性：已检查，16 项断言实质有效
  - e2e mock 被测主体：已检查，e2e 直接调真实 fs，无 mock
  - tautology：未发现
  - 跨 feature 复用：N/A（首个 feature）
- **抽测 e2e**：`web/e2e/feature_1/test1_init_workspace.ts` — 22/22 通过

## History
- [2026-05-25] Module 2 feature list created (4 features), dispatched Feature #1 to Dev
- [2026-05-25] Feature #1 accepted, dispatched Feature #2
- [2026-05-25] Feature #2 accepted, dispatched Feature #3
- [2026-05-25] Feature #3 accepted, dispatched Feature #4
- [2026-05-25] Feature #4 accepted — Module 2 all features done, triggering UAT
- [2026-05-28] Feature #5 added (bug fix: template copy __dirname issue), dispatched to Dev
- [2026-05-28] Feature #5 accepted — Module 2 all features done (including bug fix)
- [2026-05-29] Feature #6 added (启动 Daemon 按钮 + __dirname fix), dispatched to Dev
- [2026-05-29] Feature #6 accepted — Module 2 all features done
